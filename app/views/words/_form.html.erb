<%= form_for(@word) do |f| %>
  <% if @word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
      <% @word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= current_user.lang_a.titleize %><br>
    <%= f.text_field :word_a %>
  </div>
  <div class="field">
    <%= current_user.lang_b.titleize %><br>
    <%= f.text_field :word_b %>
  </div>

    <%= f.hidden_field :know, value: false %>

    <%= f.hidden_field :user_id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
